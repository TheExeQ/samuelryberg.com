import { FC } from "react";

const Home: FC = () => (
  <main className="h-screen">
    <div className="flex h-full items-center justify-center">
      <p>Home</p>
    </div>
  </main>
);

export default Home;
